[package]
name = "mneme-gate"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Context gate attestation verifier (fail-closed digest guard)"

[features]
default = []
phase_ii_gate_open = []

[dependencies]
mneme-core = { workspace = true, features = ["experimental_context_gate"] }
mneme-context.workspace = true
