# TCB provenance VO skip (alias)

Canonical write-up: [`PHASE_I_TCB_FAILOPEN_PROVENANCE.md`](PHASE_I_TCB_FAILOPEN_PROVENANCE.md).

**Status:** FIXED on `redteam/tcb-failopen` via `verify_semantic_receipt_tcb_gate` (always Merkle membership; provenance path uses `verify_provenance_attestation` with `object_bytes`).

**Tag note:** `phase-i-software` may predate this fix; do not move the tag unless release policy explicitly allows a post-Phase-I security patch tag.
